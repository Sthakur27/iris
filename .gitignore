# Secrets — never commit. `.api` holds the Anthropic key used by the dev-server
# coaching endpoint.
.api
.api*
.env
.env.*
!.env.example

# Editor swap/backup files can contain a verbatim copy of the secrets above.
*.swp
*.swo
*~

node_modules/
dist/
.DS_Store
*.log
